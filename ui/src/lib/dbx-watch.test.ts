import {describe,it,expect,vi} from 'vitest'; import {startDBXWatch} from './dbx-watch'
describe('DBX watch',()=>it('parses split newline events and closes',async()=>{const invoke=vi.fn().mockImplementation(async(m:string)=>m==='kube/discover'?{resources:[{resource:'pods',version:'v1',group:'',namespaced:true}]}:m==='resource/watch'?{sessionId:'s'}:m==='resource/watch-read'?{data:'{"type":"ADDED","object":',closed:false}: {closed:true}); const events:any[]=[]; const stop=startDBXWatch('c','pods','default',{invoke},e=>events.push(e),()=>{}); stop(); await new Promise(r=>setTimeout(r,5)); expect(invoke).toHaveBeenCalled();}))
