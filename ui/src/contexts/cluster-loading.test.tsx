import { QueryClient,QueryClientProvider } from '@tanstack/react-query';import {render,screen} from '@testing-library/react';import {describe,it,expect} from 'vitest';import {ClusterProvider,ClusterContext} from './cluster-context';
describe('DBX loading',()=>it('waits for DBX bridge without HTTP cluster discovery',()=>{Object.defineProperty(window,'dbxPlugin',{value:{ready:new Promise(()=>{}),context:{}},configurable:true});render(<QueryClientProvider client={new QueryClient()}><ClusterProvider><ClusterContext.Consumer>{v=><div role="status">{v?.isLoading?'loading':'loaded'}</div>}</ClusterContext.Consumer></ClusterProvider></QueryClientProvider>);expect(screen.getByRole('status')).toHaveTextContent('loading')}));
