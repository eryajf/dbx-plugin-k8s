schema_version = 1

[backend]
language = "go"
directory = "backend"
binary = "dbx-plugin-kubernetes"

[package]
include = ["assets", "ui/dist"]

[dev]
ui_build = ["npm", "--prefix", "ui", "run", "build"]
ui_watch = ["npm", "--prefix", "ui", "run", "build:watch"]
